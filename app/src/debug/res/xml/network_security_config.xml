<?xml version="1.0" encoding="utf-8"?>
<!-- Debug-only override: permit cleartext to common local dev hosts and the
     shared backend IP, so -PsyncBaseUrl can point at a local server without
     editing the (restrictive) production config in src/main. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">34.62.20.204</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>      <!-- emulator host loopback -->
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
