*.iml
.gradle/
.kotlin/
.idea
local.properties
.DS_Store
build/
captures/
.externalNativeBuild
.cxx
*.apk
*.aab
.vscode

# Gradle wrapper JAR is automatically downloaded on first run
# If not present, run (from the android/ directory): cd android && ./gradlew --version
# (on macOS/Linux) or cd android && gradlew.bat --version (on Windows)

# Secrets — never commit
backend/k8s/secret.yaml
backend/.env

# Firebase app config (project-specific, not tracked)
/android/app/google-services.json

# Build artifacts
/android/app/release/

# Compiled Go server binary
/backend/server
